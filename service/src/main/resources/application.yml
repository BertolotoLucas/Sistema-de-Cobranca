server:
  port: 8080
  servlet:
    context-path: /v1
spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sistemaCobranca?createDatabaseIfNotExist=true&serverTimeZone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
    type: com.zaxxer.hikari.HikariDataSource
  thymeleaf:
    mode: html
    cache: false